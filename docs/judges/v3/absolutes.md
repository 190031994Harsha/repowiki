# Judge v3: absolutes (moonshotai/kimi-k3)
_2026-08-31 13:31 UTC_

The user wants an absolute-claim audit of README.md and AGENTS.md. They mention the project "just got bitten by one false absolute ('cannot contain a citation the tree doesn't support' while render() had an unresolved branch)". They want me to extract EVERY absolute or near-absolute claim, classify each as literally true / technically-true-but-misleading / false, given how the code works. But wait — I only have README.md and AGENTS.md as evidence. I don't have the actual code. So I need to reason from the documents themselves — internal inconsistencies, hedges, claims that contradict each other — and identify which absolutes are most vulnerable. The user says "given how the code works" but I only have the docs; however, the docs contain internal contradictions that reveal false absolutes without needing code. The best answer extracts claims, and flags the ones that the README's own text undermines.

Let me enumerate every absolute or near-absolute claim in both documents.

README.md:

1. Subtitle/lead: "every non-obvious claim carries a verifiable citation (file path + line range), internal links are checked, and consistency is scored deterministically."
   - "every non-obvious claim" — this is an absolute scoped by "non-obvious," which is undefined. Who decides what's non-obvious? Also, the honesty note says sentences with unresolvable citations are dropped — but the claim-support precision is 0.74, meaning 26% of claims are NOT supported by their citations. So "every non-obvious claim carries a verifiable citation" might be literally true about citation *presence*, but the citation's validity/support is not guaranteed. Also "verifiable" — a citation that resolves but doesn't support the claim is still "verifiable" in the sense you can check it. Hmm. The word "verifiable" is doing work: a citation to real code that doesn't support the claim is "verifiable" (you can verify it's wrong). Technically-true-but-misleading.

2. Table row: "never mutates the analyzed repo" — ingest read-only. Matches AGENTS.md "Agents never mutate the analyzed repository." Plausibly true (clone to cache, read-only local) — but "read-only local" depends on code not shown. Can't verify from docs. Classify as: unverifiable from evidence, but internally consistent. Actually the user asked "given how the code works" — I don't have code. I should note which claims can't be checked from the docs, and which are contradicted by the docs themselves. The strongest findings are internal contradictions.

3. "cite-by-symbol: the LLM cites [[sym:qual.name]], a deterministic AST index attaches the exact line range" — "exact" claim. But: "careful regex scanners for JS/TS/Java/Go/Rust" — regex scanners do not produce exact spans. So "exact" holds only for Python (ast). For the other 5 languages, line ranges come from regex heuristics. "exact line range" is language-conditional — technically-true-but-misleading, or false for non-Python repos.

4. "backlinks on every page" — absolute "every page". Can't verify; orphan detection exists, so backlinks on every page is a code property. Unverifiable but plausible. Hmm, but is "backlinks on every page" contradicted? No. Flag as unverifiable absolute.

5. "content is scanned before anything reaches an LLM prompt" — "anything" is absolute. AGENTS.md: "Agents never see secrets — all content is scanned and redacted before prompt construction." This is a big one. "scanned" ≠ "secrets are removed". A regex-based secrets scanner has false negatives. The claim "Agents never see secrets" is false in the general sense — it should be "agents never see content matching our secret patterns." A novel credential format, a high-entropy string not matching patterns, a secret in a file format the scanner skips — leaks. This is a classic "never see secrets" claim a hostile judge kills: scanner-based redaction cannot guarantee zero leakage. Also "all content" — is ALL content scanned? File contents yes; but what about the repo map, file paths (a secret could be in a filename, e.g., `deploy-prod-key-AKIA...`), symbol names, commit messages? If only file *contents* are scanned, then a path containing a secret reaches the prompt unscanned. The README says "content is scanned before anything reaches an LLM prompt" — if the repo map (paths) is built from ingest without scanning, that "anything" is false. I can't verify from the docs, but the claim "never see secrets" is unfalsifiable-as-true and almost certainly false as stated — it's a best-effort scanner. Classify: false-as-stated / technically-misleading. This is a strong candidate for "the next one a hostile judge kills them with."

6. "wikis grounded in the actual AST can't silently drift, because every claim is re-verifiable against the tree it cites." — "can't silently drift" is the big one. But: (a) "every claim is re-verifiable" — no, claim-support precision is 0.74, meaning 26% of sampled claims are not supported; "re-verifiable" is true only in the trivial sense that you can re-check and find it wrong. (b) Drift: the wiki is generated at a commit; the repo moves on; the wiki doesn't update itself — the citations become stale. "Can't silently drift" — actually it CAN drift; the only defense is that drift is *detectable* because citations can be re-resolved. But "silently"? If the file changes, line ranges point at the wrong lines — re-verification against the *current* tree would fail, which is detection, not prevention. The claim conflates "detectable" with "cannot happen". A hostile judge: "The wiki drifted the moment the upstream repo merged a commit. Your citations now point at wrong lines. It's silent until someone re-runs the validator. Your claim is false." Also only Python is AST; JS/TS/Java/Go/Rust are regex — "grounded in the actual AST" is false for 5/6 languages. Classify: false as stated (misleading conflation of detectable vs. impossible + AST overclaim).

7. "Why the citations can't hallucinate (the core design decision)" — heading claim. But honesty note: "Validity does not reach 1.00" and claim-support precision 0.74. The resolver prevents hallucinated *paths/ranges* from shipping (fail-closed), but the *claims* can still hallucinate — 26% of claims unsupported by their cited spans. "Citations can't hallucinate" narrowly means citations resolve to real code. But the user's premise: they got bitten by "cannot contain a citation the tree doesn't support" while render() had an unresolved branch — i.e., the fail-closed emission path had a hole. So the whole "never ships a dangling reference" family is already bitten once.

8. "The LLM never writes line numbers." — absolute about LLM behavior. LLMs are stochastic; you cannot guarantee an LLM never writes a line number. What you can guarantee is the *pipeline rejects/ignores* them. Also the baseline is "*allowed* to attempt line numbers via grep (`_grep_upgrade`)" — wait, that's the baseline generator: it attempts line numbers via grep. Hmm, `_grep_upgrade` — the baseline attempts line numbers via grep — so the baseline pipeline does attach line numbers (derived by grep, not LLM?). "The baseline cites whole files" and is "allowed to attempt line numbers via grep". If the baseline's LLM writes `[path]` only, fine. But "The LLM never writes line numbers" — is that advanced-only? The section is about the core design. Also, an LLM asked not to write line numbers sometimes will anyway; the system presumably strips/rejects them. The claim as worded ("The LLM never writes line numbers") is a behavioral absolute about a stochastic system — technically-true-if-enforced-by-prompt-only is unprovable; in practice near-always true under temp 0 with instructions, but "never" is unguaranteed. More importantly, it's about what the LLM *writes*, which the system cannot control — only what it *accepts*. Classify: unprovable absolute; the true statement is "the pipeline never *accepts* LLM-written line numbers."

9. "Unresolvable citations are rejected and the page is sent back for repair; after bounded repair, any sentence still carrying an unresolvable citation is dropped (fail-closed) — so an emitted page never ships a dangling reference" — "never ships a dangling reference". This is the family that already bit them (render() unresolved branch). Is it still false? The user's premise says they got bitten; presumably they fixed render(). But is there another hole? The validator "re-checks every emitted citation (file exists, range in bounds) for the eval score" — post-hoc. If the validator re-checks everything emitted, then a dangling reference would be caught in the score but still shipped in the page. Validity 0.96 ≠ 1.00 — wait, they explain validity <1.00 as resolver rejections... but no: if unresolved citations never ship, then validity of *emitted* citations should be 1.00 by construction. Why is it 0.96? Their explanation: "Validity does not reach 1.00 because the resolver rejects citations the index can't back, and after bounded repair the sentence is dropped (fail-closed), not shipped." Hmm, that explains dropped claims, not invalid shipped citations. If dropped sentences don't count, validity of what ships should be 1.00. Unless validity is measured over citations *attempted* (including dropped), or there are shipped citations that fail validation — i.e., holes in fail-closed. 0.96 with 4 losses... Actually a hostile judge asks: "If nothing unresolvable ships, why is emitted-citation validity 0.96 and not 1.00? Either your fail-closed claim is false, or your metric counts rejected citations — pick one; either way something in the README is misleading." That's a sharp internal tension. This is a candidate for the "next kill": the sentence "an emitted page never ships a dangling reference" sits next to a 0.96 validity number whose own footnote doesn't add up.

Wait — maybe validity denominator includes dropped claims' citations? The honesty note says exactly that, sort of: "the resolver rejects citations the index can't back... the sentence is dropped" — so validity <1 because the metric presumably counts citations across attempts including dropped? But "Citation validity (resolve to real code)" 0.91 baseline — baseline has no resolver; its invalid citations ship. For advanced, if fail-closed works, shipped = 1.00. The note implies the 0.96 is measured including the repair pipeline's rejects... Actually a plausible reading: validity measured on all citations the LLM emitted across the run (including pre-repair ones in trajectories). But the table says "Citation validity (resolve to real code)" for emitted pages? Ambiguous. The judge-kill: the 0.96↔"never ships dangling" tension. But the honesty note does try to preempt it. Still, it's contradictory or at best confusingly defined.

10. "A post-hoc validator re-checks every emitted citation (file exists, range in bounds) for the eval score — validity is a measurement, not a hope." — "every emitted citation" — absolute, unverifiable without code, but plausible. "validity is a measurement, not a hope" — fine.

11. "A separate claim-support pass then checks whether each cited span actually supports its sentence" — "checks whether each cited span" — "each" absolute. But the eval says claim-support precision measured on "75 claims" — a sample, not every claim. Is the pass run on every claim in every page, or sampled? "75 claims, 0 contradicted" suggests sampled evaluation. If the claim-support pass runs per-page on all claims, then 75 is just the eval sample. Unverifiable, but the "each" is fine if the pass exists. However the deeper problem: claim-support precision 0.74 means 26% of shipped claims are unsupported — so "every non-obvious claim carries a verifiable citation" is true in letter (citation exists) but the citation doesn't back the claim 26% of the time.

12. "0 contradicted" — 75 claims, 0 contradicted, but precision 0.74 means 19 claims were neither supported nor contradicted? That reads oddly — 0.74 precision with 0 contradicted implies categories like "unclear/unsupported-but-not-contradicted". Not an absolute claim per se. Skip or note.

13. "Measured over 12 public repos × 2 modes" vs earlier "Evaluation on ≥10 repos (one challenging)... over 10 public repos" — 10 vs 12 inconsistency! The problem-mapping table says "over 10 public repos (fastapi = the challenging metaprogramming case)", the results say "Measured over 12 public repos × 2 modes". Minor factual inconsistency; not an absolute claim but a judge-noticeable discrepancy. Worth flagging as corroborating evidence of doc rot.

14. "Aggregates computed from report.json by script; every metric shown, regressions included" — "every metric shown" absolute; unverifiable, plausible.

15. "every citation expands to the exact source lines" (showcase) — "every", "exact". Same exactness caveat (regex languages), and showcase is `requests` (Python), so fine there. But "every citation expands" — in the HTML receipt, do dropped claims appear? Fine.

16. "no server needed" — true for static HTML presumably. Fine.

17. "temp 0" — determinism-adjacent claim: "consistency is scored deterministically" — the scorer is deterministic, fine. But temperature 0 doesn't guarantee deterministic LLM outputs (known fact: temp 0 reduces but doesn't guarantee bitwise determinism, especially across providers). The README doesn't claim deterministic generation though. AGENTS.md: "every score is recomputed deterministically from artifacts a judge can inspect directly." — "every score" absolute; the *scorer* is deterministic given artifacts, plausible and defensible. OK.

18. AGENTS.md: "authored the deterministic layer..., both generators, the eval harness, and this documentation, across a single working session" — not absolute; fine.

19. "Every call is logged: system prompt, user prompt, response, token counts, cost, latency, and every citation-resolution and repair event." — "Every call is logged" absolute. Unverifiable without code; a common failure: retries/streaming/errors not logged; cached calls; the claim-support pass calls — are those logged? If claim-support is an LLM call and it's not in trajectories, "every in-product LLM call" is false. The problem-mapping table says trajectories contain "every in-product LLM call, citation resolution and repair, per run". Candidate but unverifiable from docs. The claim-support pass appearing only as a JSON suggests maybe... speculation. Flag as unverifiable-but-load-bearing.

20. "Agents never mutate the analyzed repository — ingest is read-only (clones land in a temp cache), and no tool exists that writes to the target tree." — strong absolute. The justification is structural ("no tool exists that writes"). For a local directory ingest, "read-only" — but does anything write a cache file, `.repowiki` dir, `__pycache__` from importing? Parsing Python with `ast` doesn't execute, so no pycache. Reading doesn't mutate. Git clone to temp — doesn't touch original for URL. For local: read-only claim plausible. One subtlety: atime updates on reads — pedantic. "Never mutates" content-wise plausible. I'd rate this the most defensible "never". But "no tool exists that writes to the target tree" is checkable in code — from docs, can't falsify. Note as likely-true.

21. "Agents never see secrets — all content is scanned and redacted before prompt construction; findings are reported per run." — see #5. "all content" — does the repo map / file tree / file *paths* go through secrets.py? README says "content is scanned" — content, singular type. If a file is named `.env.production.secret` or a path contains a token, paths reach prompts (repo map). Also redaction-by-regex misses novel secrets. "Never see secrets" is an outcome claim about adversarial reality; the defensible claim is "never see *detected* secrets." A hostile judge: put `AWS_SECRET=...` in an unusual format, or a base64 blob, or a secret in a binary-ish file the scanner skips, or in a filename — agents see it. FALSE as stated; technically-true-but-misleading at best ("never see secrets that match our patterns in scanned content types"). Strong candidate.

22. "Agents cannot fabricate citations that survive — the deterministic resolver rejects any symbol/path not in the index, the page fails closed (unbacked sentences are dropped), and the scorer re-validates everything emitted." — This is the biggie and the direct analog of the one that already bit them. The user's framing: "Find the NEXT one a hostile judge kills them with. Quote it exactly." The previous kill was "'cannot contain a citation the tree doesn't support' while render() had an unresolved branch" — i.e., an absolute about fabrication-survival falsified by a code path that skips validation. The NEXT one in the same family: "Agents cannot fabricate citations that survive" in AGENTS.md, and README's "an emitted page never ships a dangling reference". Why would this still be false? The validator checks "file exists, range in bounds" — that's it. A citation can be in-bounds but point at the *wrong* symbol (regex scanner mis-span), or a file-level citation `[path]` where path exists — trivially survives, and a fabricated claim with a real file citation survives claim-validity. "Fabricate citations that survive" — file-level citations from the baseline: any real path survives; LLM can attach a fabricated claim to a real file. Also: the claim-support pass is described as eval-only ("for the eval score"), not a gate — so unsupported claims ship (0.74 precision proves 26% of shipped claims' citations don't support them!). So "cannot fabricate citations that survive" is FALSE in the strong reading: fabricated *claim-citation pairings* survive at rate ~26%; only fabricated *pointers* (nonexistent symbols/paths) are rejected. The README itself refutes the AGENTS.md absolute: claim-support precision 0.74 means more than a quarter of claims are backed by citations that don't support them — i.e., hallucinated grounding that survived. The hostile judge quote: "Agents cannot fabricate citations that survive" — killed by their own 0.74.

Actually, let's be careful about definitions. "Fabricate a citation" = cite something not in the tree (fake path/symbol/line). The resolver kills those (modulo render()-style holes). "Fabricate a claim with a real citation" — survives. The AGENTS.md sentence is specifically about citations, and the resolver does reject any symbol/path not in the index — so on the narrow reading it might be true *if* there's no unresolved branch in the emit path. But the user just told us render() HAD an unresolved branch — the class of bug is "a path in the pipeline that emits without validation." Post-repair, is there another? From docs alone, the evidence that shipped-but-unsupported exists at 26% shows "survive" in the broader sense is rampant. And the 0.96 (not 1.00) shipped-citation validity — if that 0.96 is over *emitted* citations, then 4% of shipped citations fail validation, directly falsifying "never ships a dangling reference" AND "cannot fabricate citations that survive." Their footnote tries to explain 0.96 via dropped sentences, but dropped sentences' citations shouldn't be "emitted." So either the metric includes non-emitted citations (misleading metric definition) or fail-closed leaks (false absolute). Either way a judge wins a point. THE quote: which single one?

The user asks: "Find the NEXT one a hostile judge kills them with. Quote it exactly." Singular. Candidates:

A. AGENTS.md: "Agents cannot fabricate citations that survive — the deterministic resolver rejects any symbol/path not in the index, the page fails closed (unbacked sentences are dropped), and the scorer re-validates everything emitted." — same family as last bite; the 0.96↔never contradiction plus 0.74 claim-support kills "survive" semantics. But they might argue it narrowly.

B. AGENTS.md: "Agents never see secrets — all content is scanned and redacted (repowiki/secrets.py) before prompt construction." — scanner-based redaction can never support "never see secrets." This is the classic false absolute in security marketing. A hostile judge drops one weird-format secret (or a secret in a filename/path, which likely isn't "content" scanned) and the claim dies. This is arguably THE next kill because it's a different subsystem (so "next", not a re-litigation of citations), it's a security claim (judges love killing security absolutes), and "all content" is almost certainly false in code (repo map paths, the clone URL itself — a GitHub URL can contain a token! `https://x-access-token:TOKEN@github.com/...` — the URL goes where? Into logs/trajectories/prompts? The ingest takes "public GitHub URL" — but users paste token-bearing URLs; is the URL scanned? It lands in trajectory metadata plausibly. Even juicier: trajectories record system+user prompts; if a secret was missed, it's now persisted in trajectories/*.jsonl — "Agents never see secrets" becomes "secrets are archived per-run".)

C. README: "wikis grounded in the actual AST can't silently drift, because every claim is re-verifiable against the tree it cites." — "can't silently drift" conflates detectable-with-rerun with cannot-drift; and "every claim is re-verifiable" is refuted by their own 0.74/26% unsupported and by the fact that "re-verifiable" requires running a validator nobody runs automatically. Also "the actual AST" is Python-only. A hostile judge: "I edited one function after generation; your wiki drifted instantly and silently until someone re-ran the pipeline. Your claim confuses 'detectable' with 'impossible'." Also "every claim" — dropped claims? non-obvious claims? The lead says "every non-obvious claim carries a verifiable citation" but 0.74 precision means those citations verify-existence, not truth.

D. README: "The LLM never writes line numbers." — behavioral absolute about an LLM; unguaranteeable; and the baseline's `_grep_upgrade` exists precisely because numbers appear. Hmm, grep upgrade is the tool adding numbers. The LLM instructed never to... "never writes" is about output compliance; at temp 0 with clear instructions it's reliable but not guaranteed, and one malformed `[[sym:foo.py:12-14]]`... the resolver would reject it, so harm is contained; the claim itself is about the LLM though. Hostile judge could probe with an adversarial repo (a symbol named like `foo:12`)... eh, weaker.

E. "so an emitted page never ships a dangling reference" — README step 3. Directly the same claim that bit them. The user says they got bitten by "cannot contain a citation the tree doesn't support" — that's not verbatim in these docs; closest current equivalents: "an emitted page never ships a dangling reference" (README) and "Agents cannot fabricate citations that survive" (AGENTS.md). If the render() bug is fixed, these might now be true *for the resolver path* — but the claim-support 0.74 means citations that don't support their claims ship routinely. "Dangling reference" narrowly = unresolvable. Narrowly possibly-true now. But given the 0.96 validity ≠ 1.00 tension, a judge can still attack. However, the user wants the NEXT one — implying a different claim than the citation-survival family, most likely.

Ranking for "the NEXT kill": The secrets absolute (B) is the strongest because:
- It's an outcome absolute ("never") about a probabilistic detection layer — structurally unguaranteeable. Regex/heuristic secret scanners have false negatives by definition; no amount of code correctness makes "never see secrets" true.
- "all content is scanned" — the repo map includes file paths; paths are content that reaches prompts; are paths scanned? README says "content is scanned" — ambiguous, but the table says "content is scanned before anything reaches an LLM prompt" — if paths aren't scanned, false. Even if they are, pattern-misses remain.
- The ingest URL can embed credentials.
- Secrets that evade the scanner get *persisted into trajectories* ("Every call is logged: system prompt, user prompt, response...") — so the failure mode isn't just exposure to the model, it's archival.
- It's a security claim — the worst kind to be absolute and wrong about.

The drift claim (C) is the second-strongest: "can't silently drift" is philosophically false (detectable ≠ impossible; and silence persists until someone re-runs). Plus "grounded in the actual AST" is false for 5 of 6 languages (regex). And "every claim is re-verifiable" vs 0.74.

The "every non-obvious claim carries a verifiable citation" — "non-obvious" is an escape hatch, but the escape hatch itself is the problem: nothing in the pipeline can distinguish obvious from non-obvious claims deterministically; the claim is unfalsifiable-by-design, which a judge reads as marketing. Also undermined by 0.74.

Now the user wants: extract EVERY absolute/near-absolute claim, classify each (literally true / technically-true-but-misleading / false), then name the next kill with exact quote.

I don't have the code, so for many I must grade on (a) internal evidence from the docs themselves, (b) structural impossibility, (c) unverifiable-but-load-bearing (I should note when a grade depends on code I can't see, but still grade using the docs' own admissions). The docs contain enough self-refutation to grade the big ones. I'll be explicit that grades rest on the provided docs + stated premise (the render() bug), and mark evidence quality.

Let me build the full extraction list, quoted exactly:

README.md:
1. "every non-obvious claim carries a verifiable citation (file path + line range)" — lead.
2. "internal links are checked" — not absolute per se; "consistency is scored deterministically" — fine, likely true (deterministic scorer). Not absolute.
3. "never mutates the analyzed repo" (table).
4. "a deterministic AST index attaches the exact line range" — "exact".
5. "backlinks on every page" — "every page".
6. "content is scanned **before** anything reaches an LLM prompt" — "anything".
7. "trajectories/*.jsonl — every in-product LLM call, citation resolution and repair, per run" — "every".
8. "wikis grounded in the actual AST can't silently drift, because every claim is re-verifiable against the tree it cites." — "can't", "every".
9. "Why the citations can't hallucinate" — heading absolute.
10. "`ast` for Python — exact spans" — "exact" (true for Python; the broader claim context implies all).
11. "careful regex scanners for JS/TS/Java/Go/Rust" — not absolute; rather the admission.
12. "The LLM **never writes line numbers**."
13. "an emitted page never ships a dangling reference"
14. "A post-hoc **validator** re-checks every emitted citation" — "every".
15. "A separate claim-support pass then checks whether each cited span actually *supports* its sentence" — "each".
16. "Measured over 12 public repos" vs "over 10 public repos" (consistency flag, not absolute).
17. "Aggregates computed from `report.json` by script; every metric shown, regressions included" — "every metric".
18. "every citation expands to the exact source lines" (showcase).
19. "The +0.75 depth delta is partly definitional" — wait, table says +0.64! "Citations with exact line ranges 0.03 → 0.67, Δ +0.64" but the honesty note says "The +0.75 depth delta is partly definitional." 0.64 ≠ 0.75. Another internal inconsistency! Great catch — the honesty note references a +0.75 delta that matches nothing in the table (+0.64). Also "validity +0.04" in the note vs table "+0.05". And "0.91 → 0.96" is +0.05, note says +0.04. These numeric inconsistencies in the *honesty notes* are delicious: the section promising honesty contains two numbers that don't match its own table. Not absolute claims, but strong judge-bait and evidence the docs drifted from the data — ironic given "can't silently drift."
20. "Validity does not reach 1.00 because..." — the explanation that doesn't add up (dropped sentences shouldn't lower validity of emitted citations).
21. "0 contradicted" with 0.74 precision — category weirdness.
22. Quickstart: "no server needed" — fine.

AGENTS.md:
23. "both disclosed with trajectories" — fine.
24. "authored the deterministic layer ... across a single working session on 2026-08-28" — not absolute.
25. "Every call is logged: system prompt, user prompt, response, token counts, cost, latency, and every citation-resolution and repair event." — "Every".
26. "Agents **never mutate the analyzed repository**" + "no tool exists that writes to the target tree" — absolute but structurally defended.
27. "Agents **never see secrets** — all content is scanned and redacted ... before prompt construction" — the kill.
28. "Agents cannot fabricate citations that survive — ... the page fails closed ... and the scorer re-validates everything emitted." — "cannot", "everything".
29. "every score is recomputed deterministically from artifacts a judge can inspect directly" — "every score". Caveat: claim-support precision comes from an LLM judge presumably ("A separate claim-support pass" — is it an LLM pass? It says "pass then checks whether each cited span actually supports its sentence" — almost certainly LLM-based, temp 0). Then "every score is recomputed deterministically" is FALSE for the LLM-judged metrics (readability! claim-support!). Readability 0.73/0.56 — how is readability scored? Almost certainly LLM-judged; temp-0 LLM scoring is not deterministic in the strong sense (provider nondeterminism), and "recomputed deterministically from artifacts" can't apply to an LLM-judged score unless the judge outputs are cached in artifacts. If trajectories contain every call, recomputation could replay... no, replay re-calls the LLM. Unless scores are computed from stored judge outputs — then it's "recomputed from recorded scores," not recomputed. So "every score is recomputed deterministically" overclaims: deterministic metrics (validity, coverage, links) yes; LLM-judged (readability, claim-support) no. Technically-true-but-misleading or false depending on implementation. Good secondary kill candidate.

Also: "temperature 0" — claims determinism-adjacent but doesn't claim determinism. Fine.

"conistency is scored deterministically" (lead) — same caveat as #29: validity/coverage deterministic; readability/claim-support LLM-judged. The lead's "consistency is scored deterministically" refers to internal consistency (citations, links) — plausibly fine.

Now grade each:

Literally true (as best can be told from docs):
- "never mutates the analyzed repo" / "no tool exists that writes" — structurally defensible (clone-to-cache, read-only local). Grade: literally true (assuming code matches; nothing in docs contradicts). Note the caveat: ingest of local dir read-only is a code property; docs are internally consistent.
- "`ast` for Python — exact spans" — true (ast gives exact lineno/col_offset).
- "consistency is scored deterministically" (the deterministic checks) — true for the mechanical checks.
- "internal links are checked", "validity is a measurement" — true.
- "no tool exists that writes to the target tree" — plausible/structural; true-by-design.
- "no server needed" — true.

Technically-true-but-misleading:
- "every non-obvious claim carries a verifiable citation" — citation presence ≠ support (0.74); "non-obvious" undefined escape hatch; "verifiable" = you can verify it's wrong.
- "a deterministic AST index attaches the exact line range" — AST is Python-only; for JS/TS/Java/Go/Rust it's regex spans, "exact" is aspirational. Table says "deterministic AST index" while §1 admits regex scanners. Misleading generalization.
- "Why the citations can't hallucinate" — citations-as-pointers mostly can't ship fake targets (resolver), but grounding hallucination (real citation, unsupported claim) ships at ~26%. Heading overclaims; body partially walks it back.
- "The LLM never writes line numbers" — you can't guarantee LLM behavior; you can only guarantee the pipeline ignores them; also baseline `_grep_upgrade` injects LLM-unverified line numbers into baseline output — wait, `_grep_upgrade` is the tool adding numbers to baseline citations, so baseline *output* contains line numbers not written by the LLM but also "can't verify them". The claim is about the advanced pipeline. Grade: technically-true-if-lucky; the enforceable claim is "the resolver only accepts symbol/path citations." Misleading as a behavioral guarantee.
- "an emitted page never ships a dangling reference" — narrow sense (unresolvable symbol/path) plausibly enforced post-fix; but 0.96 validity sits oddly; and "dangling" excludes "resolves-but-wrong" (regex span drift, in-bounds-but-wrong-lines). Technically true under a narrow reading the reader won't supply.
- "re-checks every emitted citation (file exists, range in bounds)" — true but the check is shallow: in-bounds ≠ correct span; existence ≠ support. Misleading by what it omits; also post-hoc ("for the eval score") = detection, not gating.
- "checks whether each cited span actually supports its sentence" — if the pass runs per-page, fine; but it's not a gate (0.74 ships). "checks" true; implied consequence false.
- "every citation-resolution and repair event" logged / "Every call is logged" — unverifiable; plausible; but note claim-support pass calls must also be logged or "every in-product LLM call" fails. Grade: unverifiable-from-docs; likely-true-if-instrumented; flag.
- "backlinks on every page" — unverifiable; plausible.
- "every metric shown, regressions included" — undermined by numeric inconsistencies (+0.64 vs +0.75; +0.05 vs +0.04; 10 vs 12 repos) — if the docs can't even consistently show the metrics, "every metric shown" is shaky. Technically-true-but-undermined.
- "every citation expands to the exact source lines" (showcase, requests/Python) — true for that artifact.
- "every claim is re-verifiable against the tree it cites" — re-verifiable in principle; 26% fail support; verification requires rerunning an LLM pass (not deterministic); "every" overclaims.
- "every score is recomputed deterministically" — deterministic for mechanical metrics; LLM-judged scores (readability, claim-support precision) are not recomputable deterministically unless cached. Misleading/false.

False:
- "Agents never see secrets" — structurally unguaranteeable; scanner false-negatives; likely unscanned channels (paths/repo map, the clone URL); and logging persists misses into trajectories. FALSE as stated; the true claim is "content matching our detectors is redacted before prompts."
- "can't silently drift" — false: drift is instant upon upstream change; it's *detectable* only on re-run; silence is the default. Also "grounded in the actual AST" false for 5/6 languages. FALSE (conflation).
- Possibly "Agents cannot fabricate citations that survive" — given the render() precedent class, plus 0.96≠1.00, plus the fact that "survive" checks only existence/bounds while fabricated grounding survives at 26% — under any reading a judge picks, some word in that sentence is false ("survive", or the implied safety). Grade: false-in-spirit / technically-defensible-only-under-a-reading-the-sentence-doesn't-state. Given the user's history, this is the "current bite" family — I'll treat it as already-wounded, and name the secrets claim as the NEXT kill, with drift as runner-up... or which?

The user: "The project just got bitten by one false absolute ('cannot contain a citation the tree doesn't support' while render() had an unresolved branch). Find the NEXT one a hostile judge kills them with. Quote it exactly."

The bitten one was about citation fabrication survival. The NEXT kill — the judge, having won on citations, moves to the next absolute. Which is most killable *with the code as described*?

"Agents never see secrets" — the judge doesn't even need a code bug: the claim is false by construction (no scanner catches all secrets; "all content" almost certainly excludes paths/URLs; one counterexample input kills it). It's a live grenade in AGENTS.md, phrased as an outcome guarantee ("never see") rather than a mechanism ("content is scanned"). And the README table's version — "content is scanned **before** anything reaches an LLM prompt" — compounds it with "anything."

Also strong: "can't silently drift" — killed by pure argument (detectable≠impossible), no code needed. And "every score is recomputed deterministically" — killed if any score is LLM-judged.

I think the headline answer: the exact quote is from AGENTS.md:

"Agents **never see secrets** — all content is scanned and redacted (`repowiki/secrets.py`) before prompt construction; findings are reported per run."

Why the judge kills it: (1) redaction is a detector, not a proof — false negatives are definitional; (2) "all content" is almost certainly false — the repo map (file *paths*) is built by ingest and reaches prompts; README says "content is scanned," i.e., file contents; a token in a filename (`config/.env.prod.LeakedKey`) or in the submitted URL (`https://user:token@github.com/...` — the README invites "public GitHub URL" but users paste tokenized URLs) flows around secrets.py; (3) even for scanned contents, a base64/hex secret or a provider-specific key format the patterns don't cover passes through; (4) the miss is then *archived* — "Every call is logged: system prompt, user prompt, response" — so trajectories/*.jsonl becomes a secret store, and "findings are reported per run" reports the scanner's catches, not its misses. The defensible claim was "every prompt is built from content that passed the scanner." They wrote an outcome absolute about reality instead of a mechanism claim about their pipeline — exactly the same shape as the render() bug: the guarantee lives one layer above where the enforcement lives.

Also note: "Secrets" redaction scanning *before prompt construction* — but the baseline gets "actual file contents"? No — advanced gets file contents; scanned first. Fine. But the symbol index: docstrings, symbol names — a secret in a docstring is content, scanned? presumably if content scanned. Paths remain the cleanest kill vector, plus novel formats. I should be honest that I can't see the code; the kill stands on the impossibility of the guarantee plus the doc-internal ambiguity ("content" vs "anything"/"all content").

The verdict format: The user asked for (a) extraction of EVERY absolute/near-absolute claim with classification, (b) the next kill, quoted exactly. Format: a table or grouped list for the audit — given the number (~20), a structured list grouped by verdict, each with exact quote + one-to-two-line rationale. Then the headline: the kill quote, why it dies, what the true claim would have been, and runner-ups. Use headers, keep quotes exact. Length: substantial but tight — this is an audit; completeness matters. Aim for a thorough but non-bloated response. I'll use a table for compactness? Tables with long quotes get cramped. Maybe grouped bullet lists with bolded verdicts. I'll do: short preface (method note: grading against the docs' own admissions + the stated render() precedent; where a claim needs code to confirm, I say so), then three sections: FALSE, TECHNICALLY-TRUE-BUT-MISLEADING, LITERALLY TRUE (as far as docs show), plus a "consistency rot" note (10 vs 12 repos, +0.64 vs +0.75, +0.05 vs +0.04 — not absolutes but judge-bait proving the docs drifted, ironic). Then "THE NEXT KILL" with the exact quote and the kill chain, plus two runner-ups briefly. Also give the one-line rewrite that would survive.

Let me finalize grades item by item:

FALSE:
1. "Agents never see secrets — all content is scanned and redacted (repowiki/secrets.py) before prompt construction; findings are reported per run." (AGENTS.md) — outcome guarantee backed by a heuristic detector; "all content" contradicted by the likelihood that repo-map paths and the ingest URL aren't scanned; misses are archived by the trajectory logger. False as stated. (Even being charitable: unprovable, and one adversarial repo falsifies.)
2. "wikis grounded in the actual AST can't silently drift, because every claim is re-verifiable against the tree it cites." (README) — drift is prevented nowhere, only detectable on re-run; "silently" is the default state between runs; "actual AST" is Python-only (regex for 5 languages); "every claim is re-verifiable" is refuted by their own 0.74 claim-support (26% of claims' citations don't support them — re-verification would *fail*, which they count as a feature, but that makes the wiki *wrong*, not undrifted). False: conflates detectable with impossible.
3. "Agents cannot fabricate citations that survive — ..." (AGENTS.md) — false in the broad reading and unproven in the narrow: fabricated grounding (real cite, fake claim) survives at ~26% by their own numbers; fabricated *pointers* survive only if no unresolved branch remains — the render() precedent shows the emit path had exactly such a hole, and 0.96 shipped-validity ≠ 1.00 is unexplained by dropped sentences (dropped ≠ emitted). Grade: false until the 0.96 is reconciled; a judge makes them choose between "the metric counts non-emitted citations" (metric misleading) and "unresolved citations ship" (claim false).

Hmm — actually for #3, the honesty note tries to reconcile: "Validity does not reach 1.00 because the resolver rejects citations the index can't back, and after bounded repair the sentence is dropped (fail-closed), not shipped." If validity is computed over all LLM-attempted citations (including rejected/dropped), 0.96 makes sense and "emitted never dangling" can hold. But the table labels it "Citation validity (resolve to real code)" — ambiguous denominator. A judge pounces on the ambiguity. I'll fold this into the misleading bucket for the metric and keep #3 as "technically-defensible-only-on-a-narrow-reading / already wounded."

Also false? "content is scanned before anything reaches an LLM prompt" (README table) — "anything" makes it false if any prompt component (repo map, paths, URL, prior page content, planner inputs) bypasses the scanner. Since I can't see code, grade: false-in-any-plausible-implementation / at minimum unproven; pair with #1.

MISLEADING:
4. "every non-obvious claim carries a verifiable citation" — escape-hatch quantifier "non-obvious" (no code can classify obviousness); "verifiable" satisfied by citations that fail support (26%); a citation you can check and find wrong is "verifiable." Misleading.
5. "a deterministic AST index attaches the exact line range" — AST = Python; regex elsewhere; "exact" true only for Python repos; the eval is 10–12 public repos across languages presumably (fastapi Python... they list multi-language scanners, so repos span languages). Misleading generalization. Same for "exact spans, docstrings, call edges" — fine for Python.
6. "Why the citations can't hallucinate" — pointer-hallucination mostly prevented; grounding-hallucination ships at 26%. Heading false-as-read; body honest. Misleading.
7. "The LLM never writes line numbers." — a prompt-level hope restated as fact; the guarantee is in the resolver, not the LLM; also baseline output contains grep-upgraded line numbers the system "can't verify" — so line numbers the system can't stand behind do reach baseline pages, which contradicts the spirit. Misleading.
8. "so an emitted page never ships a dangling reference" — narrow reading maybe true post-fix; but "dangling" excludes resolves-to-wrong-span (regex drift, in-bounds-but-wrong lines), and the 0.96 denominator ambiguity undermines it. Technically-true-if-lucky.
9. "A post-hoc validator re-checks every emitted citation (file exists, range in bounds)" — checks existence/bounds only; "for the eval score" = measurement not gate; shallow check dressed as validation. Misleading by omission. (Also "every" unverifiable but plausible.)
10. "checks whether each cited span actually supports its sentence" — pass exists, but it's eval-only and 26% fail — "checks" without "enforces" while sitting in a paragraph about why citations can't hallucinate. Misleading placement.
11. "every in-product LLM call" logged / "Every call is logged: system prompt, user prompt, response, token counts, cost, latency" — plausible, unverifiable; risk points: retries, errors, the claim-support pass, cached calls. Grade: unproven absolute (I'll bucket as misleading-risk/unverifiable — better: "unverifiable from evidence; would die to one unlogged code path"). Maybe create a fourth bucket: "Unverifiable absolutes a judge will make them prove." That'd be honest: backlinks on every page; every metric shown; every call logged; every score recomputed deterministically (this one I can partially falsify: readability + claim-support are almost surely LLM-judged → not deterministically recomputed → misleading/false).
12. "every score is recomputed deterministically from artifacts a judge can inspect directly" — determinism claim extends over LLM-judged metrics (readability 0.73/0.56; claim-support 0.74). If those scores come from LLM calls, they're reproducible only from *recorded* outputs, not *recomputed*. Misleading; one-word fix ("every deterministic score") would have saved it. This is a strong runner-up kill.
13. "every citation expands to the exact source lines" — for the requests showcase (Python), fine; as a general product claim it inherits the regex-exactness issue. True-in-context.
14. "backlinks on every page" — unverifiable; plausible.
15. "every metric shown, regressions included" — undermined by the doc's own numeric drift (+0.64 table vs "+0.75" note; +0.05 table vs "+0.04" note; 10 vs 12 repos). Not false as a claim about the table, but the surrounding rot shows the docs aren't re-verified against report.json — ironic and judge-bait.
16. "validity is a measurement, not a hope" — fine, true-ish. Skip or list as true.
17. "0 contradicted" — with 0.74 precision, 19/75 claims are in a neither-bucket; not an absolute claim about the product; note briefly.

TRUE (nothing in evidence contradicts; structurally sound):
- "never mutates the analyzed repo" / "no tool exists that writes to the target tree" — enforcement is structural (no write tool), the right shape for an absolute. TRUE as far as docs show. (Pedants: atime; irrelevant.)
- "consistency is scored deterministically" (mechanical checks) — true for citation/link/coverage scoring.
- "`ast` for Python — exact spans" — true.
- "internal links are checked" — true.
- "no server needed" — true.
- "clones land in a temp cache" — true per description.

Consistency rot (non-absolute but damning, supports "docs drift" narrative):
- "over 10 public repos" (mapping table) vs "Measured over 12 public repos × 2 modes" (results) — README contradicts itself on the eval's own headline.
- Table Δ +0.64 vs honesty note "+0.75 depth delta".
- Table +0.05 vs note "validity +0.04".
- These matter because the AGENTS.md closes with "every score is recomputed deterministically from artifacts a judge can inspect directly" — a judge inspecting directly finds the README's numbers don't match each other, let alone report.json.

Now, the headline kill. The prompt's setup: bitten by citation-fabrication absolute; find the NEXT. I'll present the secrets claim as the kill, because it's (a) a different subsystem, (b) an outcome-absolute about adversarial inputs, (c) falsifiable by construction, (d) security claims draw the harshest judges. Exact quote from AGENTS.md:

"Agents **never see secrets** — all content is scanned and redacted (`repowiki/secrets.py`) before prompt construction; findings are reported per run."

Kill chain:
1. "never see secrets" is a property of all possible inputs; secrets.py is a pattern detector. Detectors have false negatives by definition. One base64 blob, one nonstandard key format, one secret split across lines — claim dead. The honest claim was "all detected secrets are redacted."
2. "all content" — the pipeline's own README says "content is scanned" (file contents). The repo map — file paths — is content that reaches prompts (baseline context is "repo map only"!). Baseline mode: "Context | repo map only." If the repo map is paths+symbols and secrets.py scans *file contents*, then in baseline mode the LLM sees paths that were never content-scanned. A repo with `deploy/.env.prod.aws_secret` or `keys/do-not-commit-TOKEN.txt` — the *path itself* leaks the secret in the filename, unscanned, in every baseline prompt. Unless secrets.py also scans the tree — the docs don't say that; they say "content."
3. The intake is "public GitHub URL" — credential-bearing URLs (`https://oauth2:TOKEN@github.com/org/repo`) are the standard way people clone private repos; the URL is logged into run metadata/trajectories and nothing in the docs says URLs pass through secrets.py.
4. The blast radius is amplified by their own transparency guarantee: "Every call is logged: system prompt, user prompt, response" — a scanner miss is not just shown to the model, it's persisted to `trajectories/*.jsonl` and shipped in the submission/eval artifacts. "Findings are reported per run" reports what the scanner *found* — a run with a miss reports zero findings, which reads as "clean."
5. Same shape as the render() kill: guarantee asserted at the outcome layer, enforcement existing one layer down (resolver↔render(); scanner↔"never see"). The judge doesn't need a code bug this time — just one adversarial repo.

Safe rewrite: "Prompts are built only from content that has passed the redaction scanner; detected secrets are replaced and reported per run." (Mechanism claim, survives any single miss.)

Runner-ups (brief):
- "every score is recomputed deterministically" — dies if readability/claim-support are LLM-judged (they are; temp 0 ≠ deterministic across provider infra, and "recomputed from artifacts" can't recompute an LLM judgment).
- "can't silently drift" — dies to pure argument: detectable-on-rerun ≠ cannot-drift; wiki is stale the moment upstream merges.
- The 0.96-vs-"never ships a dangling reference" denominator ambiguity — the judge re-opens the healed wound.

Also I should be careful: the user says "given how the code works" — I only have the two docs plus the render() anecdote. I'll state that grading uses the docs' own admissions (0.74, 0.96, regex scanners, "repo map only" baseline, "every call is logged") as proxy for code behavior, and flag where a verdict requires code to confirm. That's honest and matches the evidence block they gave.

Format plan:
- One-line framing + grading method note (brief).
- Section 1: FALSE (quotes exact, why).
- Section 2: TECHNICALLY TRUE BUT MISLEADING (tight one-liners).
- Section 3: LITERALLY TRUE / defensible (short — this shows discrimination).
- Section 4: Collateral rot (numeric contradictions) — brief bullets.
- Section 5: THE NEXT KILL — exact quote, kill chain (numbered), the one-line fix.
- Runner-ups, 2–3 bullets.

Headers + bold quotes. Keep each rationale to 1–3 lines. Total maybe 700–950 words. The user is clearly technical and wants thoroughness ("Extract EVERY"), so completeness of enumeration matters more than brevity, but I'll compress rationales. Use markdown. Exact quotes preserved with original emphasis markers where feasible.

Double-check exact quotes I'll use:

- Lead: "where every non-obvious claim carries a verifiable citation (file path + line range), internal links are checked, and consistency is scored deterministically."
- Table: "never mutates the analyzed repo"
- Table: "**cite-by-symbol**: the LLM cites `[[sym:qual.name]]`, a deterministic AST index attaches the exact line range"
- Table: "backlinks on every page"
- Table: "content is scanned **before** anything reaches an LLM prompt"
- Table: "`trajectories/*.jsonl` — every in-product LLM call, citation resolution and repair, per run"
- "README-driven docs rot; wikis grounded in the actual AST can't silently drift, because every claim is re-verifiable against the tree it cites."
- Heading: "## Why the citations can't hallucinate (the core design decision)"
- "`ast` for Python — exact spans, docstrings, call edges; careful regex scanners for JS/TS/Java/Go/Rust"
- "The LLM **never writes line numbers**."
- "so an emitted page never ships a dangling reference, at the cost